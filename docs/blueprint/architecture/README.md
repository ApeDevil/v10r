# Architecture

Cross-cutting structural decisions — patterns that span multiple features and client types.

## Files

| File | Main Topics |
|------|-------------|
| **[multi-client-core.md](./multi-client-core.md)** | • How human UI, AI agents, REST API, and background jobs share the same domain logic<br>• The four invariants (no framework imports in domain modules, date serialization in route layer, etc.)<br>• AI tool definition pattern with `createTools(userId)` factory<br>• Auth per client type, error handling across surfaces<br>• Cross-domain boundary rules, extraction rules, what we chose not to do |
| **[jobs.md](./jobs.md)** | • Two job categories by trigger mechanism: scheduled (cron / `setInterval`) and manual (admin)<br>• Vendor-agnostic scheduling: the registry owns the job (`slug → { execute }`), platform adapters own the schedule<br>• Scheduler: flat `setInterval` over the registry on the persistent container (gated on `platform.persistent`); Vercel cron + `/api/cron/[job]` on serverless<br>• Registered jobs: `session-cleanup`, `log-cleanup`, `grant-request-expiry`, …; **every registered job now carries a `vercel.json` cron** (5 previously never ran)<br>• Retention jobs: `desk-retention` (30d soft-delete purge, cascades + 90d revisions), `ai-telemetry-retention` (180d), `audit-log-retention` (365d)<br>• Execution and observability: unified `job_execution` table across all trigger types<br>• Platform constraints (Vercel timeouts, Neon PgBouncer, cron gotchas)<br>• Security: auth per trigger type, hardening checklist<br>• Technology decisions: custom registry chosen; pg-boss/BullMQ/Trigger.dev rejected with rationale |
| **[workers.md](./workers.md)** | • The three unrelated things called "worker" (Web Worker / queue worker / service worker) and the tell that separates them: what does it consume?<br>• Worker vs job — pushed-and-told vs resident-and-pulling; why one needs a claim and the other does not<br>• Web Worker offload: `$lib/workers/` pure-core/impure-shell split, required `worker.format: 'es'` in Vite (breaks only in the production build), `?worker` spawning, CSP `worker-src`, transferable `ArrayBuffer` vs structured clone<br>• Queue worker over the notification outbox: the atomic `FOR UPDATE SKIP LOCKED` claim, why one statement instead of `db.transaction()` (Bun/WebSocket), why raw SQL instead of the builder, quoted `RETURNING` aliases<br>• The `attempts` fence token — how a reaped-then-re-claimed row rejects a zombie worker's late write, and why `'pending'` stays in the guard<br>• Retry policy: exponential backoff on the database clock, jitter, the `failed`/`dead` split that makes the admin panel useful<br>• The stale-claim reaper, its lease invariant, and why it is folded into the drain rather than registered as a job (Vercel Hobby daily-cron limit)<br>• Platform behaviour: identical code dormant on Vercel, running every 15s in a container<br>• Why BullMQ/pg-boss/graphile-worker stay rejected — and why `SKIP LOCKED` survives PgBouncer where advisory locks do not<br>• The PGlite testing boundary: single-connection means `SKIP LOCKED` can never actually skip |
| **[pattern-mcp.md](./pattern-mcp.md)** | • Read-only MCP server exposing the pattern library to coding agents — makes "emulate, don't clone" executable<br>• Registry-as-product: curated cards (docs+code+tests+showcases+invariants) over raw grep, `mcp/patterns.registry.json` validated by a drift guard<br>• Deterministic plan assembly (capability match → `depends_on` closure → toposort), no inference<br>• Ephemeral read-only container spawn (`--network=none`, `:ro` mount) + app-level path containment as defense in depth<br>• Zero-dependency hand-rolled JSON-RPC 2.0 (no SDK — container has no `node_modules`)<br>• Client design constraints found via live Claude Code E2E testing (forbidden tool-def fields, text-only results, stdout/stderr discipline, `${CLAUDE_PROJECT_DIR}` non-expansion)<br>• Closing pointer to the hosted HTTP surfaces (public + admin) — see `hosted-mcp.md` |
| **[hosted-mcp.md](./hosted-mcp.md)** | • Same pattern registry served read-only over HTTP at `/api/mcp/public`, plus a separate private admin MCP at `/api/mcp/admin` — two trust surfaces, one shared transport (`src/lib/server/mcp/transport.ts`) that hard-allowlists tools per registry<br>• Public endpoint: no auth, five Pattern tools, stricter `get_file_excerpt` (registry-derived allowlist + secret denylist + realpath containment), 60 req/min per IP<br>• Admin endpoint: bearer-token auth (`MCP_ADMIN_TOKEN`, constant-time compare, 401/503, never falls back to public), five demo-state tools only, 120 req/min per IP<br>• Persistent demo state (`mcp.demo_state` singleton row) — validated, versioned via optimistic compare-and-swap, best-effort audited; same domain service backs both the admin MCP tools and the protected `/admin/mcp` page<br>• Streamable-HTTP compliance (GET→405, Origin/DNS-rebinding + `MCP-Protocol-Version` + JSON-RPC envelope validation, real MCP-SDK interop test) and a build-time excerpt snapshot so all five public tools work in the Vercel artifact<br>• Required env vars, Hermes registration commands, and local curl/test procedures |
