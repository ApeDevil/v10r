

# Questions:

## Browser Memory
- browser memmory types?
- max size of each type?
- purpuose for each memory type?
- is there something like a browser memory manager module/lib?

### how to prioritize what to save in browser?
- is it possible to save UI elements (like buttons)? or only content?
- or does SSR (server side rendering) provide enough speed edge?


### how to structure background laoding?
- is it a good idea to preload content/elements in the time where use himself is not triggering any loading?
- also what would be a good aproach to rate/predict what schould be prelaoded?


## Security
- is the use of midleware for authentification & authorization the best modern aproach? whate other options are there?

## selflearnig graph RAG 
- what is the best way to define entety relationships (relational paterns)?
- how is metadata handeld in a graph DB, does each node & edge has its own metadata?
- what is the cleanest way to create nodes & edges?

## UI libs & Styling
- what is the most flexible & light weight ui libs
- is there anything better than unoCSS that would fit our project?
- markdown rendering in. svelte + syntax extention?

## AI intergration
- ai services for sveltekit (val/huggingface ?)
