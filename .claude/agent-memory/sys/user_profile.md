---
name: user-profile
description: Who the user is and how to collaborate — solo dev on v10r reference/sandbox, expects runtime-flow rigor and file:line precision
metadata:
  type: user
---

User: stas-k@gmx.de (git ApeDevil). Solo developer of Velociraptor (v10r), a full-stack SvelteKit 2 / Svelte 5 / Bun reference + test sandbox.

- Deep full-stack expertise. Works through specialized sub-agents (sys = me, runtime/dynamic architecture).
- Expects precise, behavior-first analysis: named entry/terminating edges, failure modes, state ownership, file:line citations.
- For design/brainstorm tasks, wants structured memos returned as the final message — NOT written to .md report files.
- No backward-compat concerns (dev project, no production users) — design freely without migration shims.
